# name: Flutter Android CI

# on:
#   push:
#     branches: [ main ]
#   pull_request:
#     branches: [ main ]

# jobs:
#   build_and_test_android:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v3

#       - name: Set up Java
#         uses: actions/setup-java@v3
#         with:
#           distribution: 'zulu'
#           java-version: '17'

#       - name: Set up Flutter
#         uses: subosito/flutter-action@v2
#         with:
#           channel: 'stable'
#           flutter-version: '3.24.1' # Specify your desired Flutter version

#       - name: Get dependencies
#         run: flutter pub get

#       - name: Analyze code
#         run: flutter analyze

#       - name: Run tests
#         run: flutter test

#       - name: Build Android APK
#         run: flutter build apk --split-per-abi

#       - name: Upload Android Artifact
#         uses: actions/upload-artifact@v3
#         with:
#           name: android-build
#           path: build/app/outputs/apk/release/app-*.apk


name: Flutter Android CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build_and_test_android:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          flutter-version: '3.24.1' # Specify your desired Flutter version

      - name: Get dependencies
        run: flutter pub get

      - name: Analyze code
        run: flutter analyze

      - name: Run tests
        run: flutter test

      - name: Build Android APK
        run: flutter build apk --split-per-abi

      - name: Upload Android Artifact
        uses: actions/upload-artifact@v3
        with:
          name: android-build
          path: build/app/outputs/apk/release/app-*.apk